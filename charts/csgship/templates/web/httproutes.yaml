{{- /*
Copyright OpenCSG, Inc. All Rights Reserved.
SPDX-License-Identifier: APACHE-2.0
*/ -}}

{{- $service := include "common.service" (dict "ctx" . "service" "web") | fromYaml }}
{{- $gatewayConfig := include "common.gateway.config" (dict "ctx" . "service" $service) | fromYaml }}
{{- if and $gatewayConfig.enabled (not .Values.global.chartContext.isBuiltIn) }}
{{- $serviceName := include "common.names.custom" (list . $service.name) }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ $serviceName }}
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/service: {{ $service.name }}
    {{- if $service.labels -}}
      {{- toYaml $service.labels | nindent 4 }}
    {{- else }}
      {{- include "common.labels" . | nindent 4 }}
    {{- end }}
spec:
  parentRefs:
    - name: listeners
      {{- if .Values.global.gateway.tls.enabled }}
      sectionName: https
      {{- else }}
      sectionName: http
      {{- end }}
  hostnames:
    - {{ include "common.domain.webAPI" . }}
  rules:
    {{- $agenticSvc := include "common.service" (dict "ctx" . "service" "web") | fromYaml }}
    {{- $agenticName := include "common.names.custom" (list . $agenticSvc.name) }}
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: {{ $serviceName }}
          port: {{ dig "service" "port" 8000 $service }}
    - matches:
        - path:
            type: PathPrefix
            value: /agentic
      backendRefs:
        - name: {{ $agenticName }}
          port: {{ dig "service" "port" 8000 $agenticSvc }}
  {{- end }}
