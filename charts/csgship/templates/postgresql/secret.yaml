{{- /*
Copyright OpenCSG, Inc. All Rights Reserved.
SPDX-License-Identifier: APACHE-2.0
*/ -}}

{{- if and .Values.global.postgresql.enabled (not .Values.global.chartContext.isBuiltIn) }}
{{- $service := include "common.service" (dict "service" "postgresql" "global" .) | fromYaml }}
{{- $serviceName := include "common.names.custom" (list . $service.name) }}
{{- $existingSecret := lookup "v1" "Secret" .Release.Namespace $serviceName }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $serviceName }}
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/service: {{ $service.name }}
    {{- include "common.labels" . | nindent 4 }}
  annotations:
    helm.sh/resource-policy: keep
type: Opaque
data:
  POSTGRES_USER: {{ "csghub" | b64enc | quote }}
  {{- $defaultPass := include "common.randomPassword" "csghub" | b64enc }}
  {{- $secretPass := "" }}
  {{- with and $existingSecret $existingSecret.data }}
    {{- $secretPass = index $existingSecret.data "POSTGRES_PASSWORD" }}
  {{- end }}
  POSTGRES_PASSWORD: {{ $secretPass | default $defaultPass | quote }}
  {{- if $service.databases }}
  POSTGRES_MULTIPLE_DATABASES: {{ $service.databases | join "," | b64enc | quote }}
  {{- end }}
  POSTGRES_HOST_AUTH_METHOD: {{ "scram-sha-256" | b64enc }}
{{- end }}
